# `validate.failureAction: Enforce` — the per-rule spelling that replaced
# `spec.validationFailureAction`. Behaves the same way here: still `fail`.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: enforce-rule
spec:
  background: true
  rules:
    - name: validate-image-repo
      match:
        any:
          - resources:
              kinds:
                - Pod
      validate:
        failureAction: Enforce
        message: "Container image does not match an allowed repository prefix."
        foreach:
          - list: "request.object.spec.containers[]"
            anyPattern:
              - image: "registry.example.com/*"
